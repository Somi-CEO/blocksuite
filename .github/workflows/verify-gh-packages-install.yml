name: Verify GH Packages Install

# Smoke: install published @somi-co/blocksuite-* packages.
on:
  workflow_dispatch:
    inputs:
      version:
        description: Package version to fetch
        required: true
        default: 0.19.5-somi.0

permissions:
  contents: read
  packages: read

jobs:
  verify:
    if: github.repository == 'somi-co/blocksuite'
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com
          scope: '@somi-co'

      - name: Install store + presets + blocks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN || secrets.GITHUB_TOKEN }}
          PKG_VERSION: ${{ github.event.inputs.version }}
        run: |
          mkdir -p /tmp/pkgtest && cd /tmp/pkgtest
          npm init -y
          npm install \
            "@somi-co/blocksuite-store@${PKG_VERSION}" \
            "@somi-co/blocksuite-presets@${PKG_VERSION}" \
            "@somi-co/blocksuite-blocks@${PKG_VERSION}"
          node -e "console.log(require('@somi-co/blocksuite-store/package.json').version)"
          node -e "console.log(require('@somi-co/blocksuite-presets/package.json').version)"
          node -e "console.log(require('@somi-co/blocksuite-blocks/package.json').version)"
